# 科大讯飞语音识别API集成说明

## 概述
本次集成基于科大讯飞的WebSocket语音识别API，将语音转文字功能集成到数字人聊天系统中。

## 集成内容

### 1. 新增服务类
- **文件路径**: `ruoyi-common/ruoyi-common-digital/src/main/java/org/ruoyi/common/digital/service/XfyunVoiceRecognitionService.java`
- **功能**: 使用WebSocket实现实时语音识别
- **特点**: 
  - 基于用户提供的科大讯飞demo进行改造
  - 支持实时音频数据流处理
  - 异步处理语音识别结果
  - 完整的错误处理和日志记录

### 2. 配置更新
- **文件路径**: `ruoyi-common/ruoyi-common-digital/src/main/resources/application-xfyun.yml`
- **新增配置**: `api-secret` 用于语音识别API的鉴权

### 3. 依赖更新
- **文件路径**: `ruoyi-common/ruoyi-common-digital/pom.xml`
- **新增依赖**:
  - `com.squareup.okhttp3:okhttp:4.12.0` (升级到支持WebSocket的版本)
  - `com.google.code.gson:gson:2.10.1` (用于JSON处理)

### 4. 服务集成
- **文件路径**: `ruoyi-modules/ruoyi-digital/src/main/java/org/ruoyi/digital/service/impl/DigitalHumanChatServiceImpl.java`
- **修改内容**:
  - 注入新的语音识别服务
  - 更新 `recognizeAudioFromFile` 方法实现
  - 简化依赖导入

## 使用方式

### 配置参数
确保在 `application-xfyun.yml` 中正确配置以下参数：

```yaml
xfyun:
  app-id: 你的应用ID
  api-key: 你的API密钥
  api-secret: 你的API密钥（用于鉴权）
```

### API调用
语音识别功能会在用户上传语音消息时自动触发：

1. **前端上传语音文件**: 通过 `DigitalHumanChatRequest` 的 `audioFile` 字段
2. **消息类型设置**: 将 `messageType` 设置为 "2" 表示语音消息
3. **自动识别**: 系统会自动调用语音识别服务将音频转换为文本
4. **后续处理**: 识别结果会作为用户消息继续后续的聊天流程

### 代码流程
```
用户上传语音 -> processVoiceInput() -> recognizeAudioFromFile() 
-> XfyunVoiceRecognitionService.recognizeAudio() -> WebSocket连接科大讯飞API 
-> 返回识别结果文本 -> 继续聊天流程
```

## 技术特点

### 1. 实时处理
- 使用WebSocket建立持久连接
- 音频数据分帧发送(每40ms发送1280字节)
- 实时接收识别结果

### 2. 错误处理
- 连接超时处理(60秒)
- WebSocket连接失败重试
- 详细的错误日志记录

### 3. 性能优化
- 异步处理避免阻塞
- 使用CompletableFuture管理异步结果
- 合理的音频分帧大小和发送间隔

## 音频要求

### 支持格式
- **编码**: RAW PCM
- **采样率**: 16000 Hz
- **声道数**: 1 (单声道)
- **位深度**: 16 bit
- **格式**: 未压缩的音频数据

### 建议
- 音频文件大小建议在10MB以内
- 音频时长建议在60秒以内
- 确保音频质量清晰，避免环境噪音

## 注意事项

1. **API配额**: 注意科大讯飞API的调用次数限制
2. **网络连接**: 确保服务器能够访问科大讯飞的WebSocket端点
3. **音频格式**: 目前仅支持PCM格式的音频文件
4. **中文识别**: 当前配置为中文普通话识别，可根据需要调整语言设置

## 后续扩展

1. **多语言支持**: 可以根据用户设置动态调整识别语言
2. **音频格式扩展**: 支持更多音频格式的自动转换
3. **实时语音**: 可以扩展支持实时语音通话功能
4. **语音合成**: 配合TTS服务实现完整的语音交互

## 测试建议

1. **功能测试**: 上传不同质量和长度的音频文件
2. **异常测试**: 测试网络异常、API异常等场景
3. **性能测试**: 测试并发语音识别的处理能力
4. **集成测试**: 测试完整的语音对话流程
