# 科大讯飞文字转语音(TTS)API集成说明

## 概述
在语音识别功能基础上，进一步集成了科大讯飞的文字转语音功能，实现完整的语音交互体验。当数字人回复用户消息时，可以同时提供文本和语音输出。

## 集成内容

### 1. 新增TTS服务类
- **文件路径**: `ruoyi-common/ruoyi-common-digital/src/main/java/org/ruoyi/common/digital/service/XfyunTextToSpeechService.java`
- **功能**: 
  - 基于WebSocket实现实时文字转语音
  - 支持异步处理和结果回调
  - 音频文件自动管理和清理
  - 完整的错误处理和日志记录

### 2. 音频资源访问配置
- **文件路径**: `ruoyi-admin/src/main/java/org/ruoyi/web/config/AudioResourceConfig.java`
- **功能**: 配置静态资源访问，允许前端访问生成的音频文件

### 3. 数字人聊天服务集成
- **文件路径**: `ruoyi-modules/ruoyi-digital/src/main/java/org/ruoyi/digital/service/impl/DigitalHumanChatServiceImpl.java`
- **修改内容**:
  - 注入TTS服务
  - 实现 `convertTextToSpeech` 方法
  - 在AI回复时自动生成语音文件

## 功能特性

### 1. 自动语音生成
当用户设置输出类型为语音(`outputType = "2"`)时，系统会：
1. 生成AI文本回复
2. 自动调用TTS服务将文本转换为语音
3. 保存音频文件到服务器
4. 返回音频文件访问URL
5. 同时保存文本和音频URL到数据库

### 2. 音频文件管理
- **存储路径**: `{ruoyi.profile}/audio/`
- **文件命名**: `tts_{时间戳}.pcm`
- **音频格式**: RAW PCM, 16000Hz采样率
- **访问URL**: `/audio/{文件名}`

### 3. 配置参数
使用相同的科大讯飞配置：

```yaml
xfyun:
  app-id: 你的应用ID
  api-key: 你的API密钥
  api-secret: 你的API密钥（用于鉴权）
```

### 4. TTS参数配置
当前默认配置：
- **发音人**: x4_yezi（可根据需要修改）
- **语调**: 50
- **语速**: 50
- **音频编码**: raw
- **文本编码**: UTF8

## 使用方式

### 1. 实时语音通话
调用 `voiceChat` 接口：
```java
// 自动设置outputType为"2"，同时输出文本和语音
request.setOutputType("2");
```

### 2. 普通聊天启用语音输出
在聊天请求中设置：
```java
request.setOutputType("2"); // "1"=纯文本, "2"=文本+语音
```

### 3. 前端音频播放
系统返回的消息中包含 `audioUrl` 字段：
```json
{
  "content": "这是AI的文本回复",
  "audioUrl": "/audio/tts_1732123456789.pcm"
}
```

前端可以直接使用这个URL播放音频文件。

## API接口

### 1. 核心方法
```java
// 文字转语音主方法
public String convertTextToSpeech(String text)

// 批量转换
public List<String> convertTextToSpeechBatch(List<String> textList)

// 服务可用性检查
public boolean isServiceAvailable()

// 清理过期文件
public void cleanupExpiredAudioFiles(int maxAgeInHours)
```

### 2. 错误处理
- 连接超时：10秒WebSocket连接超时
- 转换超时：30秒TTS转换超时
- 文件存储错误：自动创建目录结构
- API错误：详细的错误码和消息记录

## 技术实现

### 1. WebSocket连接流程
1. 构建鉴权URL（HMAC-SHA256签名）
2. 建立WebSocket连接到科大讯飞TTS服务
3. 发送TTS请求（包含文本和配置参数）
4. 接收音频数据流并写入文件
5. 监听状态变化，完成时返回文件URL

### 2. 异步处理
- 使用 `CompletableFuture` 管理异步结果
- WebSocket回调中处理音频数据
- 支持超时控制和错误回调

### 3. 文件管理
- 自动创建存储目录结构
- 基于时间戳生成唯一文件名
- 提供清理过期文件的工具方法
- 静态资源配置支持文件访问

## 性能优化

### 1. 连接管理
- WebSocket连接复用（当前每次新建，可优化为连接池）
- 合理的超时设置
- 及时关闭连接释放资源

### 2. 存储优化
- 定期清理过期音频文件
- 可配置的文件存储路径
- 支持文件信息查询和监控

### 3. 错误恢复
- TTS失败不影响文本消息保存
- 详细的错误日志记录
- 优雅的异常处理

## 扩展功能

### 1. 多发音人支持
可通过配置支持不同的发音人：
```java
private static final String VCN = "x4_yezi"; // 可配置化
```

### 2. 音频格式扩展
目前支持PCM格式，可扩展支持MP3等格式

### 3. 语音参数自定义
支持动态调整语调、语速等参数

### 4. 实时流式语音
可扩展支持实时语音流传输

## 监控和维护

### 1. 日志监控
- TTS转换成功/失败日志
- 音频文件生成日志
- WebSocket连接状态日志
- 性能指标记录

### 2. 定期维护
建议定期执行：
```java
// 清理24小时前的音频文件
textToSpeechService.cleanupExpiredAudioFiles(24);
```

### 3. 服务健康检查
```java
// 检查TTS服务是否可用
boolean available = textToSpeechService.isServiceAvailable();
```

## 注意事项

1. **API配额**: 注意科大讯飞TTS API的调用次数限制
2. **存储空间**: 定期清理音频文件避免磁盘空间不足
3. **网络连接**: 确保服务器能稳定访问科大讯飞API
4. **音频格式**: 前端需要支持PCM格式音频播放
5. **并发控制**: 大量并发TTS请求时注意连接数限制

## 完整的语音交互流程

1. **用户语音输入** → 语音识别 → 文本消息
2. **AI处理** → 生成文本回复
3. **文字转语音** → 生成音频文件
4. **返回结果** → 文本 + 音频URL
5. **前端播放** → 用户听到语音回复

这样就实现了完整的语音交互循环，提供了自然的对话体验。
